import React from 'react';
import useStyles from './styles.js';

const Post = () => {
  // styles for each individual post under posts
  const classes = useStyles();
  return (<h1> POST</h1>);
};

export default Post;
